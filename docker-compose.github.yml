version: '3'

services:
  docs:
    image: lattice_bc_lab/zlattice_docs:lasted
    volumes:
      - ./node_modules:/app/node_modules
      - ./docs:/app/docs
    network_mode: "host"
    working_dir: /app

