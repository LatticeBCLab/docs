version: '3'

services:
  docs:
    image: frankly98/zlattice_docs:latest
    volumes:
      - ./node_modules:/app/node_modules
      - ./docs:/app/docs
    network_mode: "host"
    working_dir: /app

